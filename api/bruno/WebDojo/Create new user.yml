info:
  name: Create new user
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3333/api/users/register
  body:
    type: json
    data: |-
      {
        "name": "Bruce Banner",
        "email": "hulk@starker.com",
        "password": "pwd123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
