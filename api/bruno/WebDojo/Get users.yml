info:
  name: Get users
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3333/api/users
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
