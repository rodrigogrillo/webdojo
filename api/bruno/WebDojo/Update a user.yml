info:
  name: Update a user
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:3333/api/users/104
  body:
    type: json
    data: |-
      {
        "name": "SpiderMan",
        "email": "spider@marcel.com",
        "password": "pws123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
